{
  "name": "capability-lint",
  "version": "0.2.0",
  "description": "Capability-based effect tracking: lint + score for TypeScript projects",
  "main": "dist/cli.js",
  "bin": {
    "capability-lint": "dist/cli.js",
    "capability-report": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "lint": "bun src/cli.ts",
    "dev": "bun --watch src/cli.ts"
  },
  "dependencies": {
    "ts-morph": "^28.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.0.0"
  },
  "files": [
    "dist/"
  ]
}
