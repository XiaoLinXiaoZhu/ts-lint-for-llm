(state: Readonly<ImmutablePlayerState>, time: number): ImmutablePlayerState => {
  const activeLine = findActiveLyricIndexAtTime(state.lyrics, time);
  return { ...state, currentTime: time, activeLine };
}
