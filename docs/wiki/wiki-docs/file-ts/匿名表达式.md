---
alias:
  - 为什么匿名
---

# 匿名表达式

【fts文件】的内容是不含函数名的表达式。这是【SSOT】的要求——名字只在文件名中存在。

支持的形式：

| 形式 | 覆盖率 |
|------|--------|
| 箭头函数 `(...) => ...` | ~92% |
| 匿名 `function*()` / `async function*()` | ~8%（JS 箭头不支持 generator） |
| 其他值表达式 | 少量 |
