---
alias:
  - 私有
  - "_前缀"
  - export
---

# 可见性

通过文件名前缀控制导出。

| 文件名 | 编译产物 |
|--------|----------|
| `fetch-user.fts` | `export const fetchUser = ...` |
| `_validate.fts` | `const validate = ...`（不 export） |

规则：文件名以 `_` 开头 → 私有；否则 → 公开。
